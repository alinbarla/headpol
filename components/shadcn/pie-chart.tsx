"use client";

/**
 * Chart primitives used by visitor analytics (bar + pie).
 * Prefer importing from `@/components/shadcn/chart` in new code.
 */
export {
  ChartContainer,
  ChartTooltip,
  ChartTooltipContent,
  ChartLegend,
  ChartLegendContent,
  ChartStyle,
  type ChartConfig,
} from "@/components/shadcn/chart";
